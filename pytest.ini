[pytest]
# Usa un directorio local para tmp_path (evita conflictos de permisos
# en el directorio temporal del sistema en Windows).
# En Linux CI no hay problema; este override solo afecta a la ejecución local.
addopts = --basetemp=.pytest_tmp
